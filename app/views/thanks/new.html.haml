.page-header
  %h1
    - if @thank.date_at.today?
      今日感謝したことをメモする
    - else
      #{@thank.date_at.strftime("%Y年%m月%d日")}に感謝したことをメモする

.row
  .span16
    = render 'form'
    = link_to 'Back', thanks_path
