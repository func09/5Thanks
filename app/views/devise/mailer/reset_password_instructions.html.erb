━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
　「5Thanks」より、ID・パスワード再発行のお知らせ	
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━　
<%= @resource.email %>様

「5Thanks」をご利用頂き、誠にありがとうございます。


↓こちらがパスワード再設定用のアドレスです。このURLにアクセスしてください。

<%= edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %>

<ご注意>	
・お使いのメールソフトによってはリンクが異常になっている場合があります。
　この場合はURLをブラウザのアドレス欄にコピー＆ペーストしてアクセスしてください。	
・一定期間経過してもログインされなかった場合は、この再設定用URLは無効となります。
　その場合はもう一度お申し込み下さい。

▼ログインはこちらより行えます。
<%= new_user_session_url %>

